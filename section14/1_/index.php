<?php

// A requisição recebe uma resposta, e ambos podem ter um body. A resposta contém um header que é constituído pelo método GET ou POST, arquivo/path solicitado (index.php) e versão do protocolo HTTP (HTTP/1.x). Basicamente uma requisição (req) é enviada e uma resposta (res) é recebida.

